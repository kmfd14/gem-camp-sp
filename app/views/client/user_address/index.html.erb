Hello
<p><%= link_to 'New Address', new_client_user_address_path %></p>

<table>
  <thead>
    <td>address</td>
  </thead>
  <% @user_addresses.each do |address| %>
    <tr>
      <td><%= "#{address.region&.name} #{address.province&.name}" %></td>
    </tr>
  <% end %>
</table>